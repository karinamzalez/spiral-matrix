var math = require('mathjs');

class spiralMatrix {
  constructor(integer) {
    this.integer = integer;
  }

  isPerfectSquare() {
    // check
    return n > 0 && Math.sqrt(n) % 1 === 0;
  }

}

module.exports = spiralMatrix;
